name: cloudcannon validate [not found]

steps:
  - ref: ../core/run-cli.toolproof.yml
  - step: I run {command}
    command: |
      npm -s run cloudcannon -- validate . 2>&1 || true
  - snapshot: stdout
    snapshot_content: |-
      ╎No CloudCannon configuration file found. Searched: cloudcannon.config.yml, cloudcannon.config.yaml, cloudcannon.config.json
